# Travel AI fronn end app

This is front end part of Travel AI. It has been written in React.js using styled-components.

To run this application simply clone the repository, cd into `front-end` folder and run
### `npm start`